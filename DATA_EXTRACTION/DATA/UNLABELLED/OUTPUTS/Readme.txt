(1) test_NoPre.csv : This is the final data used (extracted required info from json file(input folder), No duplicates, Sorted on timestamp, unpreprocessed)

(2) test.csv : This is the data used as test set for the model (pre-processed, converted to bins)
